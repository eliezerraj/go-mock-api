package config

import (
	"time"

//	"github.com/go-mock-api/internal/handlers/http/servers"

)

// ServerConfiguration :
func ServerConfiguration(start time.Time) {
//	httpServer := servers.NewHttpServer(start)
//	httpServer.StartHttpServer()
}
